// assets/controllers/coming_soon_controller.js
// Placeholder controller for the "coming soon" block.
// Currently does nothing — reserved for future enhancements
// (e.g. "notify me" email signup, countdown to release).

import { Controller } from '@hotwired/stimulus'

export default class extends Controller {
    connect() {
        // Intentionally empty for now.
    }
}